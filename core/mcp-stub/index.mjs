#!/usr/bin/env node

console.error(`
  ┌─────────────────────────────────────────────────────────────┐
  │                                                             │
  │   ui-bridge-mcp is a placeholder package.                  │
  │                                                             │
  │   The UI Bridge MCP server must be installed locally in     │
  │   your project as @ui-bridge/mcp. To run it, use the        │
  │   --no flag so npx resolves the local binary instead of     │
  │   fetching from the registry:                               │
  │                                                             │
  │     npx --no ui-bridge-mcp                                  │
  │                                                             │
  │   Setup guide → https://ui-bridge.mariohamann.com          │
  │                                                             │
  └─────────────────────────────────────────────────────────────┘
`);

process.exit(1);

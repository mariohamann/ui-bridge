{
  "name": "design-bridge",
  "private": true,
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.90.0",
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "pnpm --filter @design-bridge/docs dev",
    "dev:vite": "pnpm --filter design-bridge-demo-vite dev",
    "dev:webpack": "pnpm --filter design-bridge-demo-webpack dev",
    "dev:rspack": "pnpm --filter design-bridge-demo-rspack dev",
    "dev:astro": "pnpm --filter design-bridge-demo-astro dev",
    "dev:next": "pnpm --filter design-bridge-demo-next dev",
    "dev:nuxt": "pnpm --filter design-bridge-demo-nuxt dev",
    "docs:build": "pnpm --filter @design-bridge/docs build",
    "build": "pnpm -r build",
    "build:core": "pnpm --filter \"./core/**\" build",
    "build:integrations": "pnpm --filter \"./integrations/**\" build",
    "build:demos": "pnpm --filter \"./demos/**\" build",
    "typecheck": "pnpm -r --parallel typecheck",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "verify": "pnpm format:check && pnpm typecheck && pnpm build && pnpm test",
    "test": "pnpm build:core && pnpm build:integrations && SKIP_BUILD=true pnpm -r --workspace-concurrency=4 run test",
    "test:client": "pnpm --filter @design-bridge/client test",
    "test:components": "pnpm --filter @design-bridge/components test",
    "test:protocol": "pnpm --filter @design-bridge/protocol test",
    "test:server": "pnpm --filter @design-bridge/server test",
    "test:integrations": "pnpm --filter \"./integrations/**\" test"
  },
  "devDependencies": {
    "prettier": "^3.8.3",
    "wireit": "^0.14.12"
  }
}

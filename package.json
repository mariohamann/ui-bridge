{
  "name": "design-bridge",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "build": "pnpm --filter @design-bridge/vite-plugin build",
    "test": "pnpm build && pnpm --filter design-bridge-demo-vue-tailwind exec playwright test --headed"
  }
}
